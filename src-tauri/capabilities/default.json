{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "photo-diary default capability (main window)",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
